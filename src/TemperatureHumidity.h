#include <HDC2080.h>

#define ADDR 0x40           // Define the address of the chip

HDC2080 sensor(ADDR);       // Create the HDC sensor 

/// @brief Set up the sensor object to begin taking measurements and writing to I2C
void HDCSetup() {

}